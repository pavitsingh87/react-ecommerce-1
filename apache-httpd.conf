ServerRoot "/usr/local/apache2"
Listen 80

LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule headers_module modules/mod_headers.so
LoadModule deflate_module modules/mod_deflate.so

ServerName localhost:80

# Security Headers
Header always set X-Frame-Options "SAMEORIGIN"
Header always set X-XSS-Protection "1; mode=block"
Header always set X-Content-Type-Options "nosniff"

# Compression
<Location />
    SetOutputFilter DEFLATE
</Location>

# Proxy to Node.js backend
ProxyPreserveHost On
ProxyPass /api/ http://bijoukart-app:5000/api/
ProxyPassReverse /api/ http://bijoukart-app:5000/api/

ProxyPass /uploads/ http://bijoukart-app:5000/uploads/
ProxyPassReverse /uploads/ http://bijoukart-app:5000/uploads/

ProxyPass / http://bijoukart-app:5000/
ProxyPassReverse / http://bijoukart-app:5000/

ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 common

<IfModule mime_module>
    TypesConfig conf/mime.types
</IfModule>